// Elo V36.12 · distribuição Android desacoplada do Firebase.
const ANDROID_RELEASE = { versionName: '0.9.1-rc4', versionCode: 38 };
const REMOTE_MANIFEST = 'https://raw.githubusercontent.com/Teasyzi/Elo-app/main/android-version.json';
const isNative = !!window.Capacitor?.isNativePlatform?.();
const isAndroidWeb = !isNative && /Android/i.test(navigator.userAgent || '');
const isStandalone = !isNative && (window.matchMedia?.('(display-mode: standalone)')?.matches || navigator.standalone === true);
const PROFILE_POLL_MS = 1200;
let bannerNode=null, profileHandledForUid='', cleanupTimer=null, latestManifest=null, apkBannerActive=false, firebaseApiPromise=null;
const sleep=ms=>new Promise(r=>setTimeout(r,ms));
const esc=value=>String(value??'').replace(/[&<>"']/g,ch=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[ch]));
function suppressPwaNotificationNudge(active=true){window.eloPreferAndroidApk=!!active;const n=document.getElementById('elo-notification-nudge');if(n&&active)n.classList.add('hidden');}
function removeBanner(){bannerNode?.remove();bannerNode=null;apkBannerActive=false;}
function showBanner({title,text,primaryLabel,primaryHref,primaryAction,secondaryLabel,secondaryAction,tone='pink',apkPriority=false}){removeBanner();if(apkPriority){apkBannerActive=true;suppressPwaNotificationNudge(true);}const wrap=document.createElement('div');wrap.id='elo-android-distribution-banner';wrap.style.cssText='position:fixed;left:50%;transform:translateX(-50%);bottom:calc(5.9rem + env(safe-area-inset-bottom));z-index:2147483640;width:min(92vw,430px);font-family:Inter,system-ui,sans-serif;';const accent=tone==='green'?'#10b981':'#db2777';wrap.innerHTML=`<div style="background:rgba(15,23,42,.99);border:1px solid ${accent}55;border-radius:20px;box-shadow:0 24px 70px rgba(0,0,0,.5);padding:14px;color:#fff;backdrop-filter:blur(18px)"><div style="display:flex;gap:12px;align-items:flex-start"><div style="width:42px;height:42px;border-radius:14px;background:${accent}1c;color:${accent};display:grid;place-items:center;font-size:22px;flex:0 0 auto">📲</div><div style="min-width:0;flex:1"><div style="font-size:13px;font-weight:900;line-height:1.2">${esc(title)}</div><div style="font-size:10px;color:#94a3b8;line-height:1.45;margin-top:4px">${esc(text)}</div></div><button data-close style="border:0;background:transparent;color:#64748b;font-size:18px;padding:0 2px">×</button></div><div style="display:flex;gap:8px;margin-top:12px;flex-wrap:wrap">${primaryLabel?`<${primaryHref?'a':'button'} data-primary ${primaryHref?`href="${esc(primaryHref)}" target="_blank" rel="noopener"`:''} style="flex:1;min-width:150px;text-align:center;text-decoration:none;border:0;border-radius:13px;background:${accent};color:white;font-size:11px;font-weight:900;padding:11px 13px">${esc(primaryLabel)}</${primaryHref?'a':'button'}>`:''}${secondaryLabel?`<button data-secondary style="flex:1;min-width:130px;border:1px solid #334155;border-radius:13px;background:#0f172a;color:#cbd5e1;font-size:10px;font-weight:900;padding:10px 12px">${esc(secondaryLabel)}</button>`:''}</div></div>`;wrap.querySelector('[data-close]')?.addEventListener('click',removeBanner);const p=wrap.querySelector('[data-primary]');if(primaryAction&&p)p.addEventListener('click',primaryAction);const s=wrap.querySelector('[data-secondary]');if(secondaryAction&&s)s.addEventListener('click',secondaryAction);document.body.appendChild(wrap);bannerNode=wrap;}
async function fetchManifest(source=null){const url=source||(isNative?REMOTE_MANIFEST:new URL('./android-version.json',location.href).href);try{const sep=url.includes('?')?'&':'?';const r=await fetch(`${url}${sep}t=${Date.now()}`,{cache:'no-store'});if(!r.ok)return null;latestManifest=await r.json();return latestManifest;}catch(e){console.warn('[Elo] Manifesto Android indisponível:',e);return null;}}
async function nativeUpdate(manifest){const updater=window.Capacitor?.Plugins?.EloAppUpdater;if(!updater){location.href=manifest.downloadUrl;return;}showBanner({title:`Atualização ${manifest.versionName||''}`,text:'Preparando o download dentro do Elo…',primaryLabel:'Baixando…',secondaryLabel:'Fechar',secondaryAction:removeBanner,apkPriority:true});try{await updater.downloadAndInstall({url:manifest.downloadUrl,version:manifest.versionName||'nova'});showBanner({title:'Atualização sendo baixada',text:'O Android mostrará o progresso. Ao terminar, o Elo abrirá a instalação automaticamente.',primaryLabel:'OK',primaryAction:removeBanner,apkPriority:true});}catch(e){showBanner({title:'Não foi possível baixar',text:e?.message||'Falha ao iniciar a atualização.',primaryLabel:'Tentar novamente',primaryAction:()=>nativeUpdate(manifest),secondaryLabel:'Agora não',secondaryAction:removeBanner,apkPriority:true});}}
async function getFirebaseApi(){if(!firebaseApiPromise)firebaseApiPromise=Promise.all([import('https://www.gstatic.com/firebasejs/10.8.0/firebase-app.js'),import('https://www.gstatic.com/firebasejs/10.8.0/firebase-firestore.js')]).then(([a,f])=>({...a,...f}));return firebaseApiPromise;}
async function firebaseContext(){for(let i=0;i<30;i++){const user=window.currentUser;if(user?.uid){try{const api=await getFirebaseApi();if(api.getApps().length)return{user,db:api.getFirestore(api.getApp()),api};}catch(e){return null;}}await sleep(PROFILE_POLL_MS);}return null;}
async function getProfileState(ctx){const{collection,getDocs}=ctx.api;const snap=await getDocs(collection(ctx.db,'userProfiles',ctx.user.uid,'fcmTokens'));let nativeTokens=0;snap.forEach(d=>{const p=String(d.data()?.platform||'web').toLowerCase();if(p==='android'||p==='native')nativeTokens++;});return{nativeTokens};}
async function waitForNativeRegistration(ctx){for(let i=0;i<15;i++){try{if((await getProfileState(ctx)).nativeTokens>0)return true;}catch(_){}await sleep(1000);}return false;}
async function removeAndroidWebPushTokens(ctx){const{collection,getDocs,deleteDoc}=ctx.api;const snap=await getDocs(collection(ctx.db,'userProfiles',ctx.user.uid,'fcmTokens'));const ds=[];snap.forEach(d=>{const x=d.data()||{};if(String(x.platform||'web').toLowerCase()==='web'&&/Android/i.test(String(x.userAgent||'')))ds.push(deleteDoc(d.ref));});if(ds.length)await Promise.allSettled(ds);return ds.length;}
async function setAndroidPrimary(ctx){if(!await waitForNativeRegistration(ctx))return false;const{doc,setDoc}=ctx.api;await setDoc(doc(ctx.db,'userProfiles',ctx.user.uid),{pushPrimaryPlatform:'android',pushPrimaryUpdatedAt:Date.now(),androidAppVersionName:ANDROID_RELEASE.versionName,androidAppVersionCode:ANDROID_RELEASE.versionCode},{merge:true});await removeAndroidWebPushTokens(ctx);return true;}
async function keepPwaPushSuppressed(ctx){suppressPwaNotificationNudge(true);clearInterval(cleanupTimer);const run=()=>removeAndroidWebPushTokens(ctx).catch(()=>{});await run();cleanupTimer=setInterval(run,15000);}
async function activatePwaNotificationsAfterUninstall(ctx){try{const{doc,setDoc}=ctx.api;await setDoc(doc(ctx.db,'userProfiles',ctx.user.uid),{pushPrimaryPlatform:'web',pushPrimaryUpdatedAt:Date.now(),androidAppRemovedAt:Date.now()},{merge:true});}catch(_){}clearInterval(cleanupTimer);suppressPwaNotificationNudge(false);removeBanner();try{await window.openNotificationPermissionPrompt?.({manual:true});}catch(_){}}
async function handleAccountMigration(){const ctx=await firebaseContext();if(!ctx||profileHandledForUid===ctx.user.uid)return;profileHandledForUid=ctx.user.uid;if(isNative){try{await setAndroidPrimary(ctx);}catch(e){}return;}if(!isAndroidWeb)return;try{const{doc,getDoc}=ctx.api;const ps=await getDoc(doc(ctx.db,'userProfiles',ctx.user.uid));const profile=ps.exists()?ps.data()||{}:{};const state=await getProfileState(ctx);if(apkBannerActive)return;if(profile.pushPrimaryPlatform==='android'||state.nativeTokens>0){await keepPwaPushSuppressed(ctx);showBanner({title:'Elo Android é seu app principal',text:'As notificações deste PWA ficam desativadas enquanto você usa o APK, evitando duplicidade.',primaryLabel:'Entendi',primaryAction:removeBanner,secondaryLabel:'Desinstalei o APK · reativar PWA',secondaryAction:()=>activatePwaNotificationsAfterUninstall(ctx),tone:'green',apkPriority:true});}}catch(e){}}
async function handleDistribution(){if(isNative){const manifest=await fetchManifest(REMOTE_MANIFEST);if(manifest?.available&&Number(manifest.versionCode||0)>ANDROID_RELEASE.versionCode&&manifest.downloadUrl)showBanner({title:`Atualização ${manifest.versionName||'nova'} disponível`,text:Array.isArray(manifest.changes)&&manifest.changes.length?String(manifest.changes[0]):'Há uma nova versão do Elo para Android.',primaryLabel:'Atualizar agora',primaryAction:()=>nativeUpdate(manifest),secondaryLabel:'Agora não',secondaryAction:removeBanner,apkPriority:true});return;}if(!isAndroidWeb)return;const manifest=await fetchManifest();if(!manifest?.available||!manifest.downloadUrl)return;suppressPwaNotificationNudge(true);const text=isStandalone?'Você está usando o Elo instalado pelo navegador. O APK é a versão principal no Android. Instale-o e depois remova este atalho antigo.':'O Elo para Android está disponível. O APK é a versão principal no Android e recebe as notificações nativas.';showBanner({title:`Elo para Android ${manifest.versionName||''}`.trim(),text,primaryLabel:'Baixar Elo para Android',primaryHref:manifest.downloadUrl,primaryAction:()=>localStorage.setItem('elo_android_apk_downloaded_at',String(Date.now())),secondaryLabel:'Agora não',secondaryAction:removeBanner,apkPriority:true});}
window.eloAndroidDistribution={release:ANDROID_RELEASE,check:handleDistribution,migrateAccount:handleAccountMigration};
if(isAndroidWeb){suppressPwaNotificationNudge(true);document.addEventListener('visibilitychange',()=>{if(!document.hidden&&window.eloPreferAndroidApk)suppressPwaNotificationNudge(true);},{passive:true});}
if(isNative)document.addEventListener('visibilitychange',()=>{if(!document.hidden)setTimeout(handleDistribution,500);},{passive:true});
setTimeout(handleDistribution,350);setTimeout(handleAccountMigration,3200);window.addEventListener('elo:auth-ready',handleAccountMigration);
